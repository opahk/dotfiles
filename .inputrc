set editing-mode vi
$if mode=vi
    set keymap vi-command
    # Prefix history search, vi-command mode.
    #"j": history-search-backward
    #"k": history-search-forward
    #"gg": beginning-of-history
    #"G": end-of-history
    set keymap vi-insert
    # jj Return to command mode.
    "jj": vi-movement-mode
$endif
